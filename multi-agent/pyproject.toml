[project]
name = "multi-agent"
version = "0.1.0"
description = "Multi-agent networking event simulation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langgraph>=0.2.0",
    "langchain-google-genai>=2.0.0",
    "rich>=13.0.0",
    "python-dotenv>=1.2.1",
]
